Notes

9 to 5 (9 divs)

color code for past, present future (grey, green, white)





---------------

    { 
      timeslot: ""
      description: ""
    {

      Save to local storage.
    }



var timeBlockContainer = document.getElementById("container");
var timeBlockArray = ["1","2","3","4","5","6","7","8","9"];


    /*function createTimeBlocks() {
// Needs to Textboxes!
    for(i = 0; i < timeBlockArray.length; i++) {
        var timeBlock = document.createElement("input");
        timeBlock.setAttribute("type", "text")
        timeBlock.classList.add("time-block")
        timeBlock.textContent = "Time";
        timeBlockContainer.append(timeBlock);
    }
}
*/
/*
function createSaveButton() {
    for(i = 0; i < timeBlockArray.length; i++) {
        var saveButton = document.createElement("button");
        saveButton.classList.add("saveBtn")
        saveButton.textContent ="Save";
        timeBlockContainer.append(saveButton);
    }
}
*/